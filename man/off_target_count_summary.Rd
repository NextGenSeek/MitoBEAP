% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/off_target_count_summary.R
\name{off_target_count_summary}
\alias{off_target_count_summary}
\title{off_target_count_summary}
\usage{
off_target_count_summary(
  Adj = NULL,
  SampleList = NULL,
  lower = 0.1,
  upper = 70,
  condition_levels = c("control", "treated"),
  out_dir_base = ".",
  out_dir = NULL,
  csv_prefix = "MutationCount",
  plot_file = "MutationCount_grouped.pdf",
  make_plot = TRUE,
  combined_col_in_samplelist = "CombinedName",
  condition_col = "Condition"
)
}
\arguments{
\item{Adj}{Data frame containing adjusted off-target editing data.
Defaults to the global `Adj` object if not supplied.}

\item{SampleList}{Data frame containing sample metadata.
Defaults to the global `SampleList` object if not supplied.}

\item{lower}{minimum heteroplasmy level to take into account}

\item{upper}{maximum heteroplasmy level to take into account}

\item{condition_levels}{Default control and treated}

\item{out_dir_base}{Base analysis directory. Defaults to the current working
directory (`"."`). By default, output files are written to the `Plots`
subdirectory within this directory.}

\item{out_dir}{Optional explicit output directory. If supplied, this overrides
`out_dir_base` for backward compatibility.}

\item{csv_prefix}{Default "MutationCount"}

\item{plot_file}{pdf name. Default "MutationCount_grouped.pdf"}

\item{make_plot}{Logical. If `TRUE`, creates the grouped off-target count plot.
Default is `TRUE`.}

\item{combined_col_in_samplelist}{Character. Name of the column in
`SampleList` containing the combined sample/group name.
Default is `"CombinedName"`.}

\item{condition_col}{Character. Name of the column containing the experimental
condition. Default is `"Condition"`.}
}
\value{
A list containing per-sample counts (`counts`), grouped summary
  statistics (`stats`), and the joined input data (`data_joined`).
  When `make_plot = TRUE`, the returned list also contains the ggplot
  object (`p`).
}
\description{
What the function does.
}
